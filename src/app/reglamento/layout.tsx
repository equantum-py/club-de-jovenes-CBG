import type { Metadata } from "next";
export const metadata: Metadata = { title: "Reglamento", description: "Reglamento para participantes de Gracia Camp 2026.", alternates: { canonical: "/reglamento" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
