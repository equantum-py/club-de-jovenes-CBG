import type { Metadata } from "next";
import Header from "@/components/Header";
import { ButtonLink, Container, Eyebrow } from "@/components/ui/design";
export const metadata: Metadata = { title: "Inscripción recibida", robots: { index: false, follow: false } };
export default function Confirmacion({ searchParams }: { searchParams: { codigo?: string } }) { const codigo = searchParams.codigo || "Consultá con el equipo"; return <div className="min-h-screen bg-brand-warmWhite text-brand-ink"><Header/><main className="py-16 sm:py-24"><Container><div className="mx-auto max-w-3xl border-t border-brand-gold pt-8"><Eyebrow>Inscripción recibida</Eyebrow><h1 className="mt-4 text-4xl font-semibold text-brand-forest sm:text-6xl">¡Recibimos tu registro!</h1><p className="mt-5 text-lg leading-8 text-brand-muted">Guardá este código. Puede servirte para consultar tu inscripción.</p><p className="my-8 rounded-xl bg-brand-cream p-5 text-2xl font-semibold text-brand-forest" aria-label={`Código de inscripción: ${codigo}`}>{codigo}</p><h2 className="text-2xl font-semibold">Próximos pasos</h2><p className="mt-3 leading-7 text-brand-muted">El equipo organizador revisará la inscripción y se comunicará para confirmar el pago. Mientras tanto, leé el reglamento.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><ButtonLink href="/reglamento">Leer Reglamento</ButtonLink><ButtonLink href="/campamento" variant="secondary">Volver al Campamento</ButtonLink></div></div></Container></main></div> }
